from setuptools import find_packages
from setuptools import setup

setup(
    name="detr",
    version="0.1",
    packages=find_packages(),
    description="PyTorch DETR Object Detection Library",
    python_requires=">3.8",
)
